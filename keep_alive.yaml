# keep_alive.yaml
# Render Cron Job — Keeps Patti AI alive on free tier

services:
  - type: cron
    name: patti-ai-keep-alive
    runtime: python
    schedule: "*/10 * * * *"     # Every 10 minutes
    buildCommand: pip install requests
    startCommand: python ping.py
